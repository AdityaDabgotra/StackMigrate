# Orchestration
langgraph>=0.2.60
langgraph-checkpoint-postgres>=2.0.13
langchain-anthropic>=0.3.0

# API / async job layer
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
celery[redis]>=5.4.0
redis>=5.2.0

# DB
psycopg[binary,pool]>=3.2.0
asyncpg>=0.30.0

# VCS / GitHub integration (built in a later step)
PyGithub>=2.5.0
GitPython>=3.1.43

# Validation
pydantic>=2.9.0
pydantic-settings>=2.6.0

# Sandbox (built in a later step)
docker>=7.1.0

# Dev / test
pytest>=8.3.0
pytest-asyncio>=0.24.0
ruff>=0.7.0